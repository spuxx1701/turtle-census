{{page-title "Level composition"}}
<Panel @size="large" @title="Level composition">
    <Chart::Characters @chartId="chart-class-composition" @chartType="bar" @label="Level composition" @key="level"
        @characters={{@model.characters}} @categories={{@model.categories}} @useSpectrum={{true}}
        @spectrumStart="orange" @spectrumEnd="green" @useFactionFilter={{true}} @useRaceFilter={{true}}
        @useClassFilter={{true}} @useGuildFilter={{true}} />
</Panel>