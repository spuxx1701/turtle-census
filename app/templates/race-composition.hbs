{{page-title "Race composition"}}
<Panel @size="large" @title="Race composition">
    <Chart::Characters @chartId="chart-class-composition" @chartType="bar" @label="Race composition" @key="race"
        @characters={{@model.characters}} @categories={{@model.categories}} @useFactionFilter={{true}}
        @useClassFilter={{true}} @useGuildFilter={{true}} />
</Panel>