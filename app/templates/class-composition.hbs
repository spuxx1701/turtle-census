{{page-title "Class composition"}}
<Panel @size="large" @title="Class composition">
    <Chart::Characters @chartId="chart-class-composition" @chartType="bar" @label="Class composition" @key="class"
        @characters={{@model.characters}} @categories={{@model.categories}} @useFactionFilter={{true}}
        @useRaceFilter={{true}} @useGuildFilter={{true}} />
</Panel>